/* let nome = prompt("Olá, me diga seu nome por favor.")

let horario = prompt("Que horas são?")

if (horario > 0 && horario <= 12) {
  alert("Bom dia, " + nome + ". Agora são exatamente " + horario + "hr(s) da manhã.")
} else if(horario <= 18) {
  alert("Boa tarde, " + nome + ". Agora são exatamente " + horario + "hr(s) da tarde.")
} else if (horario <=24) {
  alert("Boa noite, " + nome + ". Agora são exatamente " + horario + "hr(s) da noite.")
}
  */